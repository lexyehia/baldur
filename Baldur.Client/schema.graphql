# This file was generated based on ".graphqlconfig". Do not edit manually.

schema {
    query: RootQuery
    mutation: RootMutation
}

type RootMutation {
    addTransaction(amount: Int, date: Int, description: String): Transaction
    createUser(email: String, firstName: String, lastName: String, password: String): User
    deleteUser(pk: Int): Boolean
}

type RootQuery {
    transaction(pk: Int): Transaction
    transactions: [Transaction]
    user(q: String): User
    users: [User]
}

"UserType returned as a DTO back through GraphQL"
type Transaction {
    date: String!
    description: String
    id: ID!
    lineItems: [JSONString]
    sum: Int!
}

"UserType returned as a DTO back through GraphQL"
type User {
    email: String
    firstName: String
    id: ID!
    lastName: String
}


"""
Allows use of a JSON String for input / output from the GraphQL schema.

Use of this type is *not recommended* as you lose the benefits of having a defined, static
schema (one of the key benefits of GraphQL).
"""
scalar JSONString