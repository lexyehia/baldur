query GET_USER {
    user(q: "1") {
        email
    }
}

mutation CREATE_USER($email: String, $password: String) {
    createUser(email: $email, password: $password, firstName: "Hello", lastName: "Hi") {
        id
    }
}